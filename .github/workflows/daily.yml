name: Daily News Update
on:
  schedule:
    - cron: '0 13 * * *'
    - cron: '0 18 * * *' 
    - cron: '0 23 * * *' 
  workflow_dispatch: {}
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.MY_PERSONAL_TOKEN }}
      - name: Set up Python
        uses: actions/setup-python@v4
        with: 
          python-version: '3.11'
      - name: Install deps
        run: pip install requests jinja2
      - name: Run updater
        run: python bot.py
      - name: Commit and push
        run: |
          git config user.name "alejhomoreno"
          git config user.email "morenoalarconalejandro052@gmail.com"
          git add README.md
          git commit -m "Automated update: $(date -u +%Y-%m-%dT%H:%M:%SZ)" || echo "no changes"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.MY_PERSONAL_TOKEN }}
